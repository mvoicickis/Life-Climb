<%= turbo_stream.update(params[:panel_id].presence || "strategist-panel") do %>
  <%= render "strategy_helps/panel",
        result: @result,
        error: @error,
        accept_as: @accept_as,
        journey_id: @journey_id,
        parent_id: @parent_id,
        life_area_id: @life_area_id,
        target_input: @target_input,
        horizon: @horizon,
        goal: @goal %>
<% end %>
