<%# Today card + Base camp overlays on the mountain photo (mockup V4 BASE_YFRAC). %>
<% today_card = local_assigns.fetch(:today_card) %>
<% day_count = local_assigns.fetch(:day_count) %>

<div class="lp-trail__dock" data-trail-ignore>
  <a href="<%= dashboard_path %>"
     class="lp-trail-today <%= 'is-busy' if today_card[:busy] %>">
    <span class="lp-trail-today__icon" aria-hidden="true">
      <svg viewBox="0 0 24 24" width="21" height="21" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
        <% if today_card[:busy] %>
          <path d="M12 3 4.5 6v6.2c0 4.4 3.1 7.6 7.5 8.8 4.4-1.2 7.5-4.4 7.5-8.8V6Z"/>
        <% else %>
          <path d="M12 3 4.5 6v6.2c0 4.4 3.1 7.6 7.5 8.8 4.4-1.2 7.5-4.4 7.5-8.8V6Z"/>
          <path d="M9.2 12.1 11.2 14.1 15.2 9.8"/>
        <% end %>
      </svg>
    </span>
    <span class="lp-trail-today__copy">
      <span class="lp-trail-today__headline"><%= today_card[:headline] %></span>
      <span class="lp-trail-today__sub"><%= today_card[:sub] %></span>
    </span>
    <% if today_card[:badge] %>
      <span class="lp-trail-today__badge"><%= today_card[:count] %></span>
    <% end %>
    <span class="lp-trail-today__chev" aria-hidden="true">›</span>
  </a>

  <div class="lp-trail-base" aria-hidden="true">
    <div class="lp-trail-base__icons">
      <span class="lp-trail-base__tent"></span>
      <span class="lp-trail-base__tent is-2"></span>
      <span class="lp-trail-base__fire"></span>
    </div>
    <span class="lp-trail-base__pill"><%= t("strategy.rpg.trail.base_camp_day", day: day_count) %></span>
  </div>
</div>
