<% content_for :title, "Edit habit" %>

<div class="mx-auto max-w-lg">
  <h1 class="mb-2 text-3xl font-bold text-slate-900">Edit habit</h1>
  <p class="mb-8 text-slate-600">Update your habit details.</p>

  <%= render "form", habit: @habit %>
</div>
