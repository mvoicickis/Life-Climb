#!/bin/bash -e

# Always prepare DB on boot (create/migrate). Safe if already up to date.
if [ -n "$DATABASE_URL" ] || [ "$RAILS_ENV" = "production" ]; then
  echo "Preparing database..."
  ./bin/rails db:prepare
fi

exec "${@}"
